import 'package:flutter/material.dart';

class LearningRecordPage extends StatelessWidget {
  const LearningRecordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('学習記録確認ページ'),
      ),
    );
  }
}